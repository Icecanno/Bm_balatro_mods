return {
    misprint_display = false,
    flip_sort = false,
    predict_random = false,
    predict_random_bonus = false,
    flash_load = false,
    manual_save = false,
    score_parse = false,
    score_display = false,
    reduce_animation = false,
}
